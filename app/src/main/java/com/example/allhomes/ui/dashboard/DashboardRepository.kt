package com.example.allhomes.ui.dashboard

import com.example.allhomes.model.PropertyResult
import com.example.allhomes.network.PropertyListService
import kotlinx.coroutines.flow.Flow

class DashboardRepository(
    private val service: PropertyListService
) {

//    suspend fun getPropertyList(mode: String): Flow<Result<List<PropertyResult>>> {
//       // return service.fetchListing(mode)
//    }
}